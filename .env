# Supabase (BYO) — no Lovable Cloud.
# Project URL + publishable key are PUBLIC values (safe in app config).
# The service_role key is SECRET and stored via Lovable Secrets (SB_SERVICE_ROLE_KEY), never here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
